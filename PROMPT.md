# 口コミ下書き生成 — デモページ 開発プロンプト

> **使い方**
> 新しい空のリポジトリで、Claude Code に最初にこのファイルを読ませる。
> 同じフォルダの `survey-presets.json` を必ず一緒に置く。
> 既存の Q-CUBE リポジトリ（variable-qr）には一切触らない。

---

## 0. あなた（Claude Code）への指示

- 最後まで読んでから着手する
- **セクション2は設計の前提。理由ごと理解し、崩さない**
- **最初に `git init` し、すべての変更をコミットしてからデプロイする。** コミットされていないコードを本番に出さない（過去に本番機能を失った事故から来る要件）
- 迷ったら勝手に決めず、セクション10を確認し、無ければ依頼者に聞く
- **APIキーをリポジトリに書かない。コミットしない。ログに出さない。** これだけは絶対

---

## 1. 作るもの

**QRを読んだ来店客が4問のアンケートに答えると、口コミの下書きが作られ、Googleなどの投稿フォームへ誘導される——その流れを見せるデモページ。**

- **静的サイト。** サーバー無し、DB無し、ログイン無し
- **GitHub Pages で公開**
- 管理画面は作らない。設定はページ内の「デモ設定」パネルで行う
- 見せたいのは**体験の流れ**。特に「下書きをコピー → 投稿フォームで貼る」の受け渡し

Q-CUBE（可変QR）にはこのページのURLを登録するだけ。Q-CUBE側の変更はゼロ。

---

## 2. 絶対に守ること（デモでも同じ）

### 2-1. 自動投稿はできない。実装してはいけない

Googleに口コミを作成するAPIは存在しない。Googleは「口コミはユーザー本人によってのみ作成できる」と公式に明示している。投稿フォームへの本文の事前入力もできない（`writereview` は `placeid` しか受け取らない）。

成立する形は**「生成 → 本人がコピー → 投稿フォームへ遷移 → 本人が貼って投稿」だけ。**

### 2-2. レビューゲーティングを作らない

**満足度で導線を分岐させない。** 低評価者を公開投稿から遠ざける実装はGoogleが禁止しており、ビジネスプロフィール停止・口コミ一括削除の対象になる。

- 何を選んでも同じ下書き画面・同じ投稿導線へ進む
- 「気になった点」は拾うが、**拾ったら下書きに必ず反映する**
- 満足度・星評価を聞く設問を作らない

### 2-3. 投稿できたかは検知できない

完了画面は「ご協力ありがとうございました」。**「投稿ありがとうございました」と書かない。**

### 2-4. 同じ文章を量産しない

同一店舗で似た口コミが並ぶとスパム判定される。対策はセクション6-3。

---

## 3. 技術構成

| 項目 | 内容 |
| --- | --- |
| ビルド | Vite + TypeScript。フレームワークは Vue 3 か素のTSか、**軽いほうを選ぶ**。SSR不要 |
| 公開 | GitHub Pages。**GitHub Actions**（`actions/deploy-pages`）で `main` への push をトリガーに `dist/` を配信 |
| 依存 | 最小限。UIライブラリは入れない。CSSは自前 |
| データ | `survey-presets.json` をビルドに同梱。設定は `localStorage` |
| AI | 既定は**テンプレート生成（APIなし）**。任意で Claude を使うモードあり（セクション6-4） |

### GitHub Pages の落とし穴

- **サブパス配信**（`https://<user>.github.io/<repo>/`）になる。Vite の `base` を `/<repo>/` に設定しないとアセットが404になる。環境変数かリポジトリ名から自動で決める
- SPA のルーティングはハッシュ（`#/survey`）にするか、単一画面で状態遷移させる。`404.html` トリックは使わない（シンプルに）
- **HTTPSで配信される**ので、クリップボードAPIの実機検証がそのままできる（これがGitHub Pagesを選ぶ利点）

---

## 4. 画面フロー

### 4-0. 入口（デモ用）

業種を選ぶ画面。8業種のカードを並べ、タップすると**その業種のデモ店舗**で下のフローが始まる。店名は「〇〇（デモ）」でよい。

右上に歯車アイコン → デモ設定パネル（セクション8）。

### 4-1. アンケート〜完了

```
[0] 「4つの質問に答えるだけ。口コミの文章は、AIがかわりに書きます」
    「約30秒で終わります」／「はじめる」
      ↓
[1] 設問1（単一選択）── タップした瞬間に次へ。「次へ」ボタンは置かない
[2] 設問2（単一選択）── 同上
[3] 設問3 よかったところ（複数・最大3）── 「次へ」
[4] 設問4 気になったところ（複数・最大2）── 先頭に「特にない」。
    「特にない」は他を外して即座に次へ（1タップで終われる）
    ── 「この内容で文章を作る」
      ↓
[5] 「文章を作っています…」（テンプレートモードでも 1〜2秒演出を入れる。即表示だとAIらしく見えない）
      ↓
[6] 下書き
    1案だけ表示。「別の言い方にする」で残り2案へ即切替
    「編集」で自由に直せる
    「この内容で〇〇に投稿する」（〇〇は投稿先名）
    小さく「AIが下書きを作成しました。実際のご体験に合うよう修正してください」
      ↓
[7] 受け渡し ★この機能の心臓部
    タップした瞬間にクリップボードへコピー
    「✓ 文章をコピーしました」
    「このあと開く画面で ①星を選ぶ ②入力欄をタップ ③貼り付け ④投稿する」
    「〇〇を開く」
    下に、長押しでコピーできる本文のテキストエリア（保険）
      ↓
[8] 投稿フォーム（別タブ）── 本人が貼って投稿
      ↓
[9] 戻ってきたら「ご協力ありがとうございました」
```

進捗ドット（4つ）を設問画面の上に置く。「もどる」は設問2以降に小さく置く。

---

## 5. アンケート

**`survey-presets.json` が正本。** 8業種、各4問。そのまま取り込む。

| 設問 | id | 形式 |
| --- | --- | --- |
| Q1 | `scene` | 単一 |
| Q2 | `context` | 単一 |
| Q3 | `good` | 複数・最大3 |
| Q4 | `concern` | 複数・最大2 |

### 「特にない」はコードで付ける

JSONの `noneOption` を、**Q4の先頭にアプリが自動で付与する**。プリセット側には入っていない。`exclusive: true` なので、選ぶと他を外して即座に次へ。

### 回答の扱い

選択肢の**IDで持ち、表示・生成時に設問定義からラベルへ解決する**。存在しないIDは無視。

---

## 6. 文章生成

### 6-1. 出力仕様

| 項目 | 値 |
| --- | --- |
| 文字数 | 120〜180字目安（許容 80〜250） |
| 文体 | です・ます調、3〜4文、一人称（「私は」と書かない） |
| 絵文字 | なし |
| 案数 | 3案。**3案は必ず異なる語り口**にする |
| 気になった点 | 選ばれていれば**必ず含める**。おだやかに。省かない |
| 禁止語 | 最高、絶対、No.1、ナンバーワン、日本一、世界一、必ず、間違いなし。金額表記（¥、円） |

### 6-2. テンプレートモード（既定・APIなし）

静的サイトなのでこれが標準。**「AIっぽく自然に見える」ことが目標**。単なる穴埋めに見えたら失敗。

構造：

```
[書き出し]  語り口シードごとに2〜3パターン用意し、Q1・Q2のラベルを埋める
[本文]      Q3の選択ラベルを、接続表現を変えながら1〜2文にする
             例：「〇〇で、△△なのがよかったです」「特に〇〇が印象に残りました」
[気になる]  Q4が選ばれていれば1文。「〇〇という点は少し気になりました」等、2〜3パターン
[締め]      語り口シードごとに2〜3パターン
```

語り口シード（12種）。**3案はここから重複なく3つ選ぶ**：

```
plain     淡々と短い文で
scene     入店時の様子から
feeling   感じたことを中心に
recommend 人にすすめる調子
casual    くだけた話し言葉
detail    具体的な場面をひとつ
reason    利用した理由から
compare   想像との違い
timeline  入店から帰るまで
who       どんな人向きか
short     要点だけ短く
warm      やわらかく落ち着いて
```

各シードで書き出し・締めの語彙と文の長さを変える。**同じ回答で3回生成しても、毎回違う文が出る**ように、パターン選択に乱数を使う。

### 6-3. 量産防止

- 語り口シードをランダムに（上記）
- 各スロットのパターンをランダムに
- **直近20件の生成文を `localStorage` に持ち、先頭30字が一致する案は作り直す**

### 6-4. Claude モード（任意・BYOK）

デモの説明者が**自分のAPIキーをその場で入れて**本物のAIで生成するモード。

| 項目 | 内容 |
| --- | --- |
| 有効化 | デモ設定パネルで「Claude を使う」をONにし、APIキーを入力 |
| キーの保存先 | **`localStorage` のみ。** リポジトリ・ビルド成果物・URL・ログのどこにも出さない |
| SDK | `@anthropic-ai/sdk`（公式）。`new Anthropic({ apiKey, dangerouslyAllowBrowser: true })` |
| なぜ `dangerouslyAllowBrowser` が許容されるか | キーが**説明者本人のもので、本人の端末から出ない**から。不特定多数に配るサイトに埋め込む用途ではない。この前提をコードコメントに書く |
| モデル | `claude-haiku-4-5` |
| パラメータ | `temperature: 1`、`max_tokens: 4000`、`thinking` は指定しない、`output_config` は送らない |
| 出力の受け取り | `strict: true` のツール `submit_review_drafts`（`input_schema`: `{ drafts: string[3] }`, `additionalProperties: false`）。`tool_choice: { type: "auto" }` にしてプロンプトでツール使用を指示。ツールが返らなければ本文のJSONも拾う |
| 失敗時 | テンプレートモードへ自動フォールバック。画面は止めない |

プロンプト（システム）：

```
あなたは、来店したお客さま本人が口コミを書くのを手伝います。
お客さまが選んだ内容だけをもとに、その人が書いたように自然な文章を作ってください。

【必ず守ること】
- 120〜180字、3〜4文
- です・ます調
- お客さまが選んでいない事実を足さない
- 店員の名前、金額、他店との比較を書かない
- 誇張した断定を使わない（最高、絶対、No.1、日本一、必ず、間違いなし など）
- 広告の文章にしない。ふつうの人が書く言葉づかいにする
- 絵文字は使わない

【気になった点の扱い】 ← 気になった点が選ばれたときだけ入れる
- 選ばれた気になった点は、必ず文章に含める。省かない
- 選ばれた表現の範囲で、おだやかに書く。強い非難にしない
- よかった点と気になった点の両方に触れ、どちらかに偏らせない

- {業種の aiHints.note があれば}
- 次の表現は使わない: {aiHints.avoid}

【今回の語り口】
- {シードの指示文}

【出力】
- 異なる語り口で3案。必ず submit_review_drafts ツールで提出する
```

プロンプト（ユーザー）：

```
業種: {業種名}
店名: {店名}

お客さまの回答:
- {Q1ラベル} → {選択}
- {Q2ラベル} → {選択}
- {Q3ラベル} → {選択、選択}

気になったところ: {選択、選択} ／ または「特になし（短所には触れないでください）」
```

**利用者の入力文字列をプロンプトに入れない。** 回答はIDで受け、ラベルへ解決してから渡す。

### 6-5. 出力の検証（両モード共通）

80〜250字、禁止語なし、金額表記なし、直近20件と先頭30字が不一致。落ちた案は作り直す。

---

## 7. 受け渡しの技術要件（ここを外すと機能しない）

| 要件 | 理由 |
| --- | --- |
| **コピーと遷移を2タップに分ける** | iOS Safari はユーザー操作の同期処理内でしかクリップボードを許さない。遷移もユーザー操作起点でないとポップアップブロックに掛かる |
| コピーは `navigator.clipboard.writeText` を**同期的に**呼ぶ（`await` を先に挟まない） | iOSで失敗する |
| **貼り方の説明を iOS / Android で出し分ける** | Android:「キーボードの上に出てくる文章をタップ」／ iOS:「ペースト」をタップ（出なければ長押し）。Androidに「長押し」と出すと目の前の候補を無視させる |
| 投稿先は `window.open(url, "_blank", "noopener")` | 元ページを残し、戻りを検知する |
| `visibilitychange` で戻りを検知して [9] へ | |
| アプリ内ブラウザ（LINE/Instagram等、UAで判定）なら「ブラウザで開き直してください」を出す | クリップボードやタブ制御が効かないことがある |
| 本文を長押し選択できる領域を常に置く | クリップボードが使えない環境の保険 |

**実機検証は GitHub Pages のURL（HTTPS）で行う。** ローカルサーバーをスマホからLAN経由（http://）で開いても `navigator.clipboard` が存在しないので検証にならない。

---

## 8. デモ設定パネル

歯車アイコンから開く。すべて `localStorage`。

| 項目 | 内容 |
| --- | --- |
| 店名 | 空なら「〇〇（デモ）」 |
| 投稿先 | 既定は `https://www.google.com/maps`（マップのトップが開くだけ。投稿はされない）。**Google Place ID を入れると** `https://search.google.com/local/writereview?placeid=...` を生成し、本物の投稿フォームが開く |
| Claude を使う | ON/OFF とAPIキー入力。OFFがテンプレートモード |
| 直近生成文をクリア | 量産防止の履歴をリセット |

Place ID 欄の下に赤字で：**「実在する店舗のPlace IDを入れると、本当に口コミが投稿できてしまいます。投稿しても差し支えない場所を指定してください。」**

APIキー欄の下に：**「キーはこの端末のブラウザにだけ保存されます。共有端末では使わないでください。」**

---

## 9. 開発の進め方

### フェーズ1：生成の核

1. `git init`、Vite プロジェクト、`survey-presets.json` 取り込み
2. 設問の正規化・「特にない」付与・回答のラベル解決（純粋関数。テストを書く）
3. テンプレート生成エンジン（純粋関数。テストを書く）
4. **ブラウザのコンソールで全業種×数パターン生成し、日本語として自然か確認する。** ここで納得できなければ画面を作っても無駄

### フェーズ2：画面

5. セクション4のフロー一式
6. GitHub Actions で Pages にデプロイ。**`base` の設定ミスで真っ白にならないか最初に確認**
7. **実機（iPhone と Android）で受け渡しを検証。** 見るのは1点：投稿フォームの入力欄をタップした瞬間にキーボードに貼り付け候補が出るか。出ないときに長押しの保険へたどり着けるか

### フェーズ3：Claude モード

8. デモ設定パネルとBYOK。テンプレートモードが動いていることを崩さない
9. 実キーで生成し、テンプレートとの質の差を依頼者に見せる

### 各フェーズの完了条件

- テストが通っている
- **コミット済み**
- 依頼者が動作を確認している

---

## 10. 未確定の判断（依頼者に確認）

| # | 項目 |
| --- | --- |
| 1 | リポジトリ名（= GitHub Pages のサブパス） |
| 2 | フレームワーク（Vue 3 か素のTSか）。依頼者に強いこだわりが無ければ**素のTS**でよい |
| 3 | 8業種に当てはまらない店舗用の汎用プリセットを足すか（デモでは不要と思われる） |
| 4 | Claude モードを最初から入れるか、後回しか |

---

## 11. 依頼者が用意するもの

| 項目 | 用途 |
| --- | --- |
| GitHub リポジトリ（空） | コードと Pages |
| 検証用の Google Place ID | 実機で本物の貼り付けを試すとき。投稿しても問題ない場所 |
| Anthropic APIキー（Claude モードを使う場合） | **デモ設定パネルに入力するだけ。エージェントに渡さない。チャットに貼らない** |

---

## 12. やらないこと

- サーバー、DB、認証、LINE連携、管理画面、課金、通知、使用量集計
- これらは本番化のときに別途設計する。デモはあくまで**体験の流れ**を見せるためのもの

---

*このプロンプトは、Q-CUBE リポジトリ内で一度実装した口コミ機能の設計・実装の知見を、静的デモ向けに切り出したもの。設計判断の理由は各セクションに埋め込んである。*
